const fetch = require('node-fetch'); // wait, node 18+ has native fetch

async function run() {
  try {
    // 1. login to get token
    // The demo user is user@cargolink.com or similar. 
    // I will check the users table if needed, or maybe write an integration test in java.
  } catch (err) {
    console.error(err);
  }
}
run();
