# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: driver.spec.ts >> Driver Journey E2E >> Driver can login, accept an order, and complete delivery
- Location: tests\driver.spec.ts:4:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected: "http://localhost:3000/driver-dashboard"
Received: "http://localhost:3000/login"
Timeout:  5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://localhost:3000/login"

```

```yaml
- region "Notifications (F8)":
  - list
- region "Notifications alt+T"
- link "Home":
  - /url: /
  - img
  - text: Home
- img
- heading "User Portal" [level=3]
- paragraph: Customer and Driver Authentication
- text: Email
- img
- textbox "Email":
  - /placeholder: name@example.com
  - text: driver@example.com
- text: Password
- link "Forgot?":
  - /url: /forgot-password
- img
- textbox "Password":
  - /placeholder: ••••••••
  - text: password123
- button:
  - img
- button "Access Dashboard":
  - text: Access Dashboard
  - img
- button "Admin"
- button "Agency"
- button "Driver"
- button "Client"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Driver Journey E2E', () => {
  4  |   test('Driver can login, accept an order, and complete delivery', async ({ page }) => {
  5  |     // 1. Login
  6  |     await page.goto('/login');
  7  |     await page.fill('input[type="email"]', 'driver@example.com');
  8  |     await page.fill('input[type="password"]', 'password123');
  9  |     await page.click('button[type="submit"]');
> 10 |     await expect(page).toHaveURL('/driver-dashboard');
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  11 | 
  12 |     // 2. Accept Order
  13 |     await page.click('text="Ordres Disponibles"');
  14 |     // Assuming there is at least one order available
  15 |     const acceptBtn = page.locator('button:has-text("Accepter")').first();
  16 |     if (await acceptBtn.isVisible()) {
  17 |       await acceptBtn.click();
  18 |       await expect(page.locator('text="Ordre accepté"')).toBeVisible();
  19 |     }
  20 | 
  21 |     // 3. Complete Delivery (Flow: Pickup -> On the Way -> Delivered)
  22 |     await page.click('text="Mes Missions"');
  23 |     const updateBtn = page.locator('button:has-text("Mettre à jour le statut")').first();
  24 |     if (await updateBtn.isVisible()) {
  25 |       await updateBtn.click();
  26 |       await page.selectOption('select[name="status"]', 'DELIVERED');
  27 |       await page.click('button:has-text("Confirmer")');
  28 |       await expect(page.locator('text="Statut mis à jour"')).toBeVisible();
  29 |     }
  30 |   });
  31 | });
  32 | 
```